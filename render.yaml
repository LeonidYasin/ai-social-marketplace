services:
  # Backend API Service (Free)
  - type: web
    name: social-marketplace-api
    runtime: node
    plan: free  # Изменено с starter на free
    buildCommand: |
      # apt-get update && \
      # apt-get install -y python3 make g++ libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev && \
      cd backend && npm install
    startCommand: cd backend && npm run init-db && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8000
      - key: DATABASE_URL
        fromDatabase:
          name: social-marketplace-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      # Syslog Server Configuration
      - key: ENABLE_SYSLOG
        value: true
      - key: SYSLOG_PORT
        value: 10000
      - key: SYSLOG_HOST
        value: 0.0.0.0
      - key: RENDER_SYSLOG_ENABLED
        value: true
      - key: RENDER_SYSLOG_ENDPOINT
        value: ${RENDER_SERVICE_URL}:10000
      - key: RENDER_SERVICE_URL
        fromService:
          name: social-marketplace-api
          type: web
          property: url
    # Disk for logs storage
    disk:
      name: logs-disk
      mountPath: /opt/render/project/src/backend/logs
      sizeGB: 1

  # Frontend Static Site (Free)
  - type: web
    name: social-marketplace-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/build
    # Environment variables are optional - the app will auto-detect URLs
    # envVars:
    #   - key: REACT_APP_API_URL
    #     value: https://social-marketplace-api.onrender.com
    #   - key: REACT_APP_WS_URL
    #     value: wss://social-marketplace-api.onrender.com

databases:
  # PostgreSQL Database (Free)
  - name: social-marketplace-db
    plan: free  # Изменено с basic-256mb на free
    databaseName: social_marketplace
    user: app_user 